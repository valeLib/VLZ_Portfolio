// SmoothScroll — approximation of the Framer marketplace SmoothScroll/Lenis wrapper.
// The original injected inertial (Lenis-style) smooth scrolling. We rely on the
// global CSS `scroll-behavior: smooth` instead, so this is a no-op placeholder
// kept for structural parity with the Framer hero.

export default function SmoothScroll(_props: { intensity?: number }) {
  return null
}
